// --- filepath: src/services/dataApi.ts
// Platzhalter für echte Datenquelle (später):
export async function fetchExternalFGI(symbol: string): Promise<number | null> {
  // TODO: implementieren – hier nur Stub
  console.debug("fetchExternalFGI called for", symbol);
  return null;
}
